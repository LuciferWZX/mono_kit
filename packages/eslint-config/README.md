# 📦 `@mono-kit/eslint-config`

工作区共享的 ESLint 配置。

## 📝 简介

这是一个基于 `@antfu/eslint-config` 的 ESLint 配置包，用于统一项目代码风格和质量检查规则。

## ✨ 特点

- 使用 `@antfu/eslint-config` 作为基础配置
- 启用 TypeScript 支持
- 统一的代码格式化规则

## 📥 使用方法

1. 在项目中安装依赖:

```shell
  pnpm --filter <target-package-name> add -D @mono-kit/eslint-config
```
